# LeaderboardManager

- 模块 ID: `leaderboard_manager`
- 层级: L2
- 依赖: player_data_manager, database_manager
- 状态: 🔲 待构建

---

## 职责

管理排行榜的数据聚合与展示。支持四个维度的排行榜（收获总量、偷菜总量、金币收入、被偷最少），按配置的刷新周期从数据库查询并缓存排名数据。提供排行榜GUI的分类选择与分页浏览，支持全服广播周期性榜单。

## 事件

发布:
- `LeaderboardRefreshedEvent` — 排行榜数据刷新完成后触发；数据: 排行榜类型、刷新时间

监听:
- `CropHarvestedEvent` — 收获事件触发时可能需要刷新缓存标记；数据: 玩家UUID、产出数量
- `CropStolenEvent` — 偷菜事件触发时可能需要刷新缓存标记；数据: 偷取者UUID、被偷者UUID、数量
- `CropSoldEvent` — 出售事件触发时可能需要刷新缓存标记；数据: 玩家UUID、总金额

## 交互

### → player_data_manager
排行榜模块调用玩家数据管理查询各维度统计值用于排名计算。

### → database_manager
排行榜模块直接调用数据库管理执行排名聚合查询并缓存结果。

### 被调用
- ← harvest_manager（详见 `modules/L2/harvest_manager.md`）
- ← placeholder_expansion（详见 `modules/L3/placeholder_expansion.md`）
- ← shop_manager（详见 `modules/L2/shop_manager.md`）
- ← steal_manager（详见 `modules/L2/steal_manager.md`）

## 关联模型

- [LeaderboardEntry](../../foundation/model.md)
