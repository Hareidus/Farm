# PlaceholderExpansion

- 模块 ID: `placeholder_expansion`
- 层级: L3
- 依赖: player_data_manager, farm_level_manager, leaderboard_manager, achievement_manager, plot_manager
- 状态: 🔲 待构建

---

## 职责

对接 PlaceholderAPI，向外部插件和记分板/全息显示插件暴露 StealFarm 的变量。提供农场等级、收获总量、偷菜总量、被偷总量、金币收入、排行榜名次、成就数量、地块大小等占位符，供聊天格式、计分板、全息图等场景使用。

## 交互

### → player_data_manager
占位符模块调用玩家数据管理查询统计值用于变量输出。

### → farm_level_manager
占位符模块调用等级管理查询农场等级用于变量输出。

### → leaderboard_manager
占位符模块调用排行榜管理查询玩家排名用于变量输出。

### → achievement_manager
占位符模块调用成就管理查询已完成成就数量用于变量输出。

### → plot_manager
占位符模块调用地块管理查询地块大小用于变量输出。
