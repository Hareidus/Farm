# ==================== 数据库配置 ====================
database:
  # 数据库类型: sqlite 或 mysql
  type: sqlite

  sqlite:
    file: data.db

  mysql:
    host: localhost
    port: 3306
    user: root
    password: ''
    database: my_plugin

# ==================== 通用配置 ====================
general:
  prefix: '&b[MyPlugin] &f'
